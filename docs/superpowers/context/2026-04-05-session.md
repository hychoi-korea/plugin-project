# 배너 플러그인 세션 요약 — 2026-04-05

## 프로젝트 위치
```
/Users/HYChoi/Documents/ai-projects/new plugin/
```

---

## 완료된 작업 (이전 세션)

### Tasks 1–8
| Task | 커밋 | 내용 |
|---|---|---|
| 1 | `c8aa1fd` | 프로젝트 인프라 (esbuild + vitest) |
| 2 | `5200915` | 공유 타입 정의 (`src/types.ts`) |
| 3 | `e66b562` | 레이어 매퍼 (`src/layerMapper.ts`) + 테스트 |
| 4 | `383f240` | 컬러 추출 (`src/colorExtractor.ts`) + 테스트 |
| 5 | `8e257a3` | 아이디어스 팔레트 (`src/palette.ts`) |
| 6 | `daccef1` | Claude API 클라이언트 (`src/claude.ts`) + 테스트 |
| 7 | `7150c2a` | Gemini API 클라이언트 (`src/gemini.ts`) + 테스트 |
| 8 | `58ebb2a` | Figma 메인 스레드 (`code.ts`) |

### Tasks 9–13
| Task | 커밋 | 내용 |
|---|---|---|
| 9 | `60c3122` | UI 셸 + API 설정 섹션 |
| 10 | `7387a83` | 이미지 입력 섹션 — 드래그앤드롭, 클립보드, 파일 선택 |
| 11 | `81960cf` | 카피 작성 섹션 — Claude AI 생성/다듬기 |
| 12 | `8b27014` | 스타일 섹션 — 뱃지, 키컬러/보색, 팔레트 |
| 13 | `23ac522` | 적용 섹션 + main.ts 최종 완성 |

---

## 오늘 작업 내용 (2026-04-05 2차 세션)

### 1. 혼합 폰트 "not a function" 에러 수정 (`code.ts`)
- **원인**: 텍스트 레이어에 혼합 폰트가 있을 때 `fontName`이 `figma.mixed`(Symbol)을 반환 → `loadFontAsync(Symbol)` 실패
- **수정**: `fontName !== figma.mixed` 체크 후 혼합 폰트면 `getRangeFontName(i, i+1)`로 문자별 폰트 개별 로드

### 2. 빈 텍스트 에러 수정 (`code.ts`)
- **원인**: `sub_copy` 등이 빈 문자열일 때 `textNode.characters = ""` → Figma 에러 → 이후 프레임 처리 중단
- **수정**: `text`가 truthy일 때만 적용 (`if (layer && type === 'TEXT' && text)`)

### 3. COMPONENT 타입 지원 (`code.ts`, `layerMapper.ts`)
- **원인**: `m_main`이 FRAME이 아닌 COMPONENT 타입 → 기존 코드가 스킵
- **수정**:
  - `getFramesFromSection` — FRAME | COMPONENT 모두 포함
  - `APPLY_CONTENT` 핸들러 — `node.type === 'FRAME' || node.type === 'COMPONENT'` 조건 추가

### 4. 레이어 매핑 정확도 수정 (`layerMapper.ts`)
- `findLayerContaining('badge')` → `Badge_group`이 먼저 걸리는 문제 → `findLayerByName('#badge')`로 변경
- `findLayerContaining` 대소문자 무시 처리 추가
- `#image_main` — containment 대신 exact name 매칭으로 변경 (`#image_main_1`과 혼동 방지)

### 5. 레이어 매핑 최종 스펙 반영 (`layerMapper.ts`, `code.ts`)

| 레이어명 | 내용 | 적용 프레임 |
|---|---|---|
| `#main_copy_01` | 메인 카피 앞줄 (최대 8자) | m_main, bottomsheet, m_landing |
| `#main_copy_02` | 메인 카피 뒷줄 (최대 8자) | m_main, bottomsheet, m_landing |
| `#main_copy_03` | 메인 카피 전체 (최대 16자) | pc_band |
| `#main_copy_04` | 요약 (최대 11자) | m_band |
| `#sub_copy` | 서브 카피 (최대 16자) | 전체 |
| `#image_main` | 메인 이미지 | 전체 |
| `#image_main_1` | 서브 이미지 1 (없으면 스킵) | 전체 |
| `#image_main_2` | 서브 이미지 2 (없으면 스킵) | 전체 |
| `#background_color` | 배경색 레이어 | 전체 |
| `#badge` | 뱃지 컴포넌트 | 전체 |

### 6. `#background_color` 레이어 지원 (`code.ts`)
- 기존: 배경색을 `frame.fills`에 직접 적용
- 변경: `#background_color` 레이어가 있으면 그 레이어 `fills`에 적용, 없으면 `frame.fills` fallback

### 7. AI 이미지 생성 기능 추가 (`src/ui/imageInput.ts`, `src/gemini.ts`)
- 이미지 입력 섹션 하단에 "✨ AI 이미지 생성 (Gemini)" 버튼 추가
- 메인 이미지 업로드 시 활성화 / Gemini API 키 필요
- 생성 완료 즉시 `APPLY_GENERATED_IMAGE` 메시지 → Figma `#image_main` 레이어에 자동 적용
- 섹션 선택 시 섹션 내 모든 프레임의 `#image_main`에 동시 적용

### 8. 이미지에서 배경색 추출 + 스와치 선택 (`src/ui/imageInput.ts`)
- 메인 이미지 업로드 시 `extractKeyColors` (4개) + `computeComplementaryColors` (2개) = 총 6개 스와치 자동 표시
- 스와치 클릭으로 배경색 선택 (재클릭 시 선택 해제 → AI 자동)
- 선택한 배경색을 Gemini 프롬프트에 명시 → 해당 색 배경으로 이미지 생성

### 9. Gemini 이미지 생성 프롬프트 개선 (`src/gemini.ts`)
- **손/신체 부위 완전 제거**: `NO HANDS, NO BODY PARTS` 명시
- **배경 톤/조도 맞춤 합성**: 배경의 조명 방향·색온도·밝기에 맞춰 제품 합성, 그림자·반사광 자연스럽게 생성
- 라이프스타일 연출 → 순수 제품 합성 컷으로 방향 변경

### 10. Gemini 모델명 현황 (`src/gemini.ts`)
- 현재 설정: `gemini-2.5-flash-image`
- ⚠️ 404 에러 발생 중 — 정확한 모델명 미확정
- **확인 방법**: 브라우저에서 아래 URL 조회 후 `generateContent` + 이미지 생성 지원 모델 선택
  ```
  https://generativelanguage.googleapis.com/v1beta/models?key=GEMINI_API_KEY
  ```

---

## 현재 파일 구조

```
new plugin/
├── manifest.json
├── code.ts                ✅ COMPONENT 지원, 혼합폰트 처리, background_color 레이어 적용
├── code.js                (빌드 결과)
├── ui.html                (빌드 결과 — ui.js 인라인)
├── ui.js                  (빌드 결과)
├── ui.template.html
├── build.js
└── src/
    ├── types.ts           ✅ APPLY_GENERATED_IMAGE 메시지 타입 추가
    ├── layerMapper.ts     ✅ 최종 레이어 스펙 반영 (exact name 매칭)
    ├── claude.ts          ✅
    ├── gemini.ts          ✅ 배경색 파라미터, 프롬프트 개선 (손 제외, 톤 합성)
    ├── colorExtractor.ts  ✅
    ├── palette.ts         ✅
    └── ui/
        ├── main.ts        ✅ getGeminiKey → initImageInput 전달
        ├── apiSettings.ts ✅
        ├── imageInput.ts  ✅ 이미지 생성 버튼, 색상 추출 스와치, Figma 자동 적용
        ├── copyWriter.ts  ✅
        ├── styleSection.ts ✅
        └── applySection.ts ✅
```

---

## 남은 작업

### 우선순위 높음
- [ ] **Gemini 모델명 확정** — API 조회 후 `src/gemini.ts`의 `GEMINI_MODEL` 수정
- [ ] **E2E QA** — 실제 플러그인 전체 흐름 검증
  - API 키 저장 → 이미지 업로드 → 색상 추출 스와치 → AI 이미지 생성 → `#image_main` 자동 적용
  - 카피 입력 → 전체 적용 → 각 프레임(m_main, m_band, pc_band, showroom, bottomsheet, m_landing) 확인
- [ ] **피그마 레이어 이미지 가져오기** — "피그마 레이어에서 가져오기" 버튼이 현재 `alert()` placeholder
  - `code.ts`에 `GET_IMAGE_FROM_LAYER` 핸들러 추가 필요
  - 선택 레이어 이미지 바이너리를 UI로 전송하는 로직

### 우선순위 중간
- [ ] `alert()` → 인라인 에러 메시지로 교체 (Figma 웹뷰에서 `alert()` 미동작 가능)
- [ ] `#main_copy_01`/`02` 분리 로직 검증 (`splitMainCopy` — 어절 경계 8자 분리)
- [ ] "이 이미지를 메인으로 사용" 버튼 UX 재검토 (자동 적용되므로 역할 중복)

### 우선순위 낮음
- [ ] 섹션 접힘/펼침 기본 상태 UX 조정
- [ ] UI 스크롤 — 팔레트 섹션 길어 640px 초과 시 스크롤 필요

---

## 주요 설계 결정사항

1. **API 키 저장**: `figma.clientStorage`
2. **이미지 생성**: Gemini API (모델명 미확정)
3. **카피 생성**: Claude API (`claude-sonnet-4-6`)
4. **컬러 추출**: Canvas API 클라이언트 사이드 (`colorExtractor.ts`)
5. **빌드**: esbuild → `ui.js` → `ui.template.html` 인라인 → `ui.html`
6. **초기화**: `DOMContentLoaded` 미사용 — 즉시 실행 IIFE
7. **섹션 토글**: 이벤트 위임 (`#app` 클릭 → `.section-title[data-target]`)
8. **생성 이미지 자동 적용**: `APPLY_GENERATED_IMAGE` 메시지 → 선택된 프레임/섹션의 `#image_main`에 즉시 반영

---

## 다음 세션 시작 방법

```
배너 플러그인 이어서 작업.
컨텍스트: docs/superpowers/context/2026-04-05-session.md 읽어줘
다음 할 일: [구체적으로 지정]
```
