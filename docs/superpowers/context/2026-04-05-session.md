# 배너 플러그인 세션 요약 — 2026-04-05

## 프로젝트 위치
```
/Users/HYChoi/Documents/ai-projects/new plugin/
```

## 완료된 작업 (Tasks 1–13 + 버그 수정)

### Tasks 1–8 (이전 세션 완료)
| Task | 커밋 | 내용 |
|---|---|---|
| 1 | `c8aa1fd` | 프로젝트 인프라 (esbuild + vitest) |
| 2 | `5200915` | 공유 타입 정의 (`src/types.ts`) |
| 3 | `e66b562` | 레이어 매퍼 (`src/layerMapper.ts`) + 테스트 |
| 4 | `383f240` | 컬러 추출 (`src/colorExtractor.ts`) + 테스트 |
| 5 | `8e257a3` | 아이디어스 팔레트 (`src/palette.ts`) |
| 6 | `daccef1` | Claude API 클라이언트 (`src/claude.ts`) + 테스트 |
| 7 | `7150c2a` | Gemini API 클라이언트 (`src/gemini.ts`) + 테스트 |
| 8 | `58ebb2a` | Figma 메인 스레드 (`code.ts`) |

### Tasks 9–13 (이번 세션 완료)
| Task | 커밋 | 내용 |
|---|---|---|
| 9 | `60c3122` | UI 셸 + API 설정 섹션 (`src/ui/main.ts` stub + `apiSettings.ts`) |
| 10 | `7387a83` | 이미지 입력 섹션 — 드래그앤드롭, 클립보드 붙여넣기, 파일 선택 |
| 11 | `81960cf` | 카피 작성 섹션 — Claude AI 생성/다듬기, 글자수 카운터 |
| 12 | `8b27014` | 스타일 섹션 — 뱃지 드롭다운, 키컬러/보색 추천, 팔레트, 피커+HEX |
| 13 | `23ac522` | 적용 섹션 + main.ts 최종 완성 (전체 섹션 조립) |

### 버그 수정
| 커밋 | 원인 | 수정 |
|---|---|---|
| `c1c96d0` | `DOMContentLoaded`가 Figma 웹뷰에서 이미 fired되어 초기화 콜백이 실행되지 않음 → 저장 버튼 클릭 불가 | `DOMContentLoaded` 래퍼 제거, 즉시 실행으로 변경. 섹션 토글도 이벤트 위임(`#app`)으로 변경 |

---

## 현재 파일 구조

```
new plugin/
├── manifest.json          ✅ networkAccess 설정 완료
├── code.ts                ✅ 메인 스레드 완성
├── code.js                ✅ 빌드 결과 (7.7K)
├── ui.html                ✅ 정적 셸 HTML
├── ui.js                  ✅ 빌드 결과 (34K)
├── build.js               ✅
├── src/
│   ├── types.ts           ✅
│   ├── layerMapper.ts     ✅
│   ├── claude.ts          ✅
│   ├── gemini.ts          ✅
│   ├── colorExtractor.ts  ✅
│   ├── palette.ts         ✅
│   └── ui/
│       ├── main.ts        ✅ 최종 완성 (즉시 초기화, 이벤트 위임)
│       ├── apiSettings.ts ✅
│       ├── imageInput.ts  ✅
│       ├── copyWriter.ts  ✅
│       ├── styleSection.ts ✅ (sendToPlugin circular import → function 호이스팅으로 정상 작동)
│       └── applySection.ts ✅
└── tests/
    ├── layerMapper.test.ts     ✅ 8 tests
    ├── colorExtractor.test.ts  ✅ 7 tests
    ├── claude.test.ts          ✅ 3 tests
    └── gemini.test.ts          ✅ 3 tests
```

**테스트: 21개 전체 통과**

---

## 레이어 매핑 규칙 (피그마 템플릿 네이밍)

| 레이어명 | 내용 | 제약 |
|---|---|---|
| `#main_copy_01` | 메인 카피 앞줄 | 최대 8자 |
| `#main_copy_02` | 메인 카피 뒷줄 | 최대 8자 |
| `#main_copy_03` | 메인 카피 전체 | 최대 16자 |
| `#main_copy_04` | m_band 요약 | 최대 11자 |
| `#sub_copy` | 서브 카피 | 최대 16자 |
| `image_main` | 메인 이미지 | — |
| `image_sub_01` | 서브 이미지 1 (없으면 스킵) | — |
| `image_sub_02` | 서브 이미지 2 (없으면 스킵) | — |
| `badge` | 뱃지 컴포넌트 (이름 매칭) | — |

---

## 남은 작업

### 우선순위 높음
- [ ] **Figma에서 E2E QA** — 저장 버튼 수정 후 실제 플러그인 전체 흐름 검증
  - API 키 저장 → 이미지 업로드 → AI 카피 생성 → 컬러 선택 → 전체 적용
- [ ] **피그마 레이어 이미지 가져오기** — 현재 `alert()` placeholder 상태
  - code.ts에 `GET_IMAGE_FROM_LAYER` 메시지 핸들러 추가 필요
  - 선택된 레이어의 이미지 바이너리를 UI로 전송하는 로직

### 우선순위 중간
- [ ] **Gemini 이미지 생성 버튼** — `copyWriter.ts` 내 AI 생성과 별개로 이미지 생성 버튼 UI 연결
- [ ] **`alert()` 제거** — Figma 웹뷰에서 `alert()`가 동작하지 않을 수 있음. 인라인 에러 메시지로 교체

### 우선순위 낮음
- [ ] UI 스크롤 확인 — 팔레트 섹션이 길어 640px 초과 시 스크롤 필요
- [ ] 섹션 접힘/펼침 기본 상태 UX 조정

---

## 주요 설계 결정사항

1. **API 키 저장**: `figma.clientStorage` (보안, Figma 공식)
2. **이미지 생성**: Gemini API (`gemini-2.0-flash-exp`)
3. **카피 생성**: Claude API (`claude-sonnet-4-6`)
4. **컬러 추출**: Canvas API 클라이언트 사이드 (API 비용 없음)
5. **빌드**: esbuild IIFE 번들 (`platform: 'browser'`)
6. **초기화**: `DOMContentLoaded` 미사용 — 스크립트가 `<body>` 맨 아래에 위치하므로 즉시 실행
7. **섹션 토글**: 이벤트 위임 (`#app` 클릭 → `.section-title[data-target]` 감지)

---

## 다음 세션 시작 방법

```
배너 플러그인 이어서 작업.
컨텍스트: docs/superpowers/context/2026-04-05-session.md 읽어줘
플랜: docs/superpowers/plans/2026-04-05-banner-plugin.md
다음 할 일: [구체적으로 지정]
```
